"use client";

// This is handled by react-hot-toast in providers.tsx
// This file is just for reference/documentation

export function TransactionToast() {
  // Toast notifications are handled globally via react-hot-toast
  // Use toast.success(), toast.error(), toast.loading() in your components
  return null;
}

